# TruckTalk Connect Environment Variables
# Copy this to .env.local for development or set in Vercel dashboard

# Required: OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: HMAC Secret for request signing (same as in Google Apps Script)
HMAC_SECRET=your_hmac_secret_here

# Optional: Allowed Origins (comma-separated URLs for CORS restriction)
# ALLOWED_ORIGINS=https://docs.google.com,https://sheets.google.com

# Optional: Rate limit (requests per minute, default: 10)
# RATE_LIMIT_RPM=10

# Optional: Log level (DEBUG, INFO, default: INFO)
# LOG_LEVEL=INFO

# Optional: OpenAI Model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Additional configuration
NODE_ENV=production
